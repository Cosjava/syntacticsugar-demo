CREATE TABLE PERSONNES (
    "IDENTIFIANT" NUMBER(10,0) NOT NULL,
    "NOM" VARCHAR2(50 CHAR) NOT NULL,
	"PRENOM" VARCHAR2(50 CHAR) NOT NULL,
	"PRESENTATION" VARCHAR2(500 CHAR) NOT NULL,
	PRIMARY KEY (IDENTIFIANT)
);
create sequence PERSONNE_SEQUENCE increment by 1 start with 100;